# OCR extraction stub
async def extract_text(image):
    # TODO: Use pytesseract or similar OCR
    # For now, return dummy UI elements
    return [
        {"text": "OK", "bbox": (100, 200)},
        {"text": "Cancel", "bbox": (200, 200)}
    ]
